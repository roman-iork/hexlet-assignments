---
server:
  port: 7070
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  output:
    ansi:
      enabled: always

# BEGIN
rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem
# END